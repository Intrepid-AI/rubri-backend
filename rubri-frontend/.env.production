# Production environment variables for React frontend
# These are baked into the build when running `npm run build`
# They can be overridden at build time via Docker build args

# Backend API URL - Update this to match your production domain
# This will be replaced at build time in Docker
VITE_API_BASE_URL=https://api.rubri.ai

# WebSocket URL - Automatically derived from API URL if not set
# VITE_WS_BASE_URL=wss://api.rubri.ai

# Disable debug mode for production
VITE_DEBUG=false